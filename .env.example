# Example only — copy values into local.properties (gitignored), not into source.
# local.properties:
#   SUPABASE_URL=https://YOUR_PROJECT.supabase.co
#   SUPABASE_ANON_KEY=your_anon_public_key
#
# Never put service_role here or in the app.
SUPABASE_URL=
SUPABASE_ANON_KEY=
